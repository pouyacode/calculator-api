(ns front-end.db)

(def default-db
  {:expression ""                       ; The expression typed by user.
   :result ""                           ; Result of calculation, from back-end.
   :loading "none"})                    ; `display` attribute of loading anim.
